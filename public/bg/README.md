Drop your two background images here:

- `scene-wide.png` — landscape composition, used by default and in landscape orientation.
- `scene-tall.png` — a separately composed portrait image (not a crop of the wide one), used in `@media (orientation: portrait)`.

Both are referenced in `app/globals.css` under `.hero-bg`.
